#!/usr/bin/env ruby

# XXX: remove this
$LOAD_PATH.unshift(File.join(File.dirname(__FILE__), '..', 'lib'))

trap("TERM") { exit(0) }
trap("INT")  { exit(0) }

begin
  require "rocket"
rescue LoadError
  require "rubygems"
  require "rocket"
end

Rocket::CLI.dispatch(ARGV)
